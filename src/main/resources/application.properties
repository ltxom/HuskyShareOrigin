## Spring DATASOURCE (DataSourceAutoConfiguration & DataSourceProperties)
spring.datasource.url = jdbc:mysql://huskyshare.com:6033/db_huskyshare?useSSL=false&amp;serverTimezone=UTC&amp;characterEncoding=utf-8
spring.datasource.username = root
spring.datasource.password = HuskY.SharE.2018TO2022

## Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQLDialect

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = update
spring.jpa.properties.connection.isolation = 4
spring.jpa.properties.hibernate.current_session_context_class = thread
spring.mvc.static-path-pattern=/static/**